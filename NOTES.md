# 📝 Личные заметки по курсу

## Занятие 3 — Владение и заимствование

Ключевые моменты:
- `i32` копируется, `String` перемещается
- `&T` — много одновременных ссылок
- `&mut T` — только одна ссылка
- Разыменование `*` нужно при присваивании нового значения

## Занятие 4 — Структуры

Ключевые моменты:
- Именно структура владеет памятью для данных
- Для форматирования строк при, например, возврате из метода, используем format!